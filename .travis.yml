os: linux
dist: xenial
language: cpp
compiler: clang
addons:
  apt:
    sources:
      - llvm-toolchain-trusty-6.0
    packages:
      - clang-6.0
env:
  - MATRIX_EVAL="CC=clang-6.0 && CXX=clang-6.0"
before_install:
  - eval "${MATRIX_EVAL}"
script:
- cmake .
- cmake --build .



